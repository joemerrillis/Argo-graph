server:
  host: 0.0.0.0
  port: 8000
  timeout: 30s

memory:
  summarization_enabled: true
  summarization_max_tokens: 1024
  embedding_enabled: true
  embedding_model: openai

postgres:
  dsn: ${ZEP_DATABASE_URL}
  pool_size: 10

auth:
  api_keys:
    - ${ZEP_API_KEY}
